spring:
  application:
    name: service-product
  cloud:
    nacos:
      config:
        server-addr: 192.168.10.20:8848 # nacos的服务端地址
        namespace: public
        group: SEATA_GROUP
    alibaba:
      seata:
        tx-service-group: product-service